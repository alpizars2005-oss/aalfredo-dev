# Changelog

## Unreleased — audit follow-up, 2026-09-25

- Keep page content visible without JavaScript and when browser storage is unavailable.
- Collapse mobile navigation only after its controls are attached; restore focus on Escape.
- Preserve ES/EN selection and both existing CV assets.
- Use Angel Alfredo as the public display name, without modifying the CV PDFs or external identity links.
- Add 11 browser regression methods and a public identity check.
- Run the browser suite over local HTTP in CI; Playwright is a test-only dependency.
- Document the distinction between local inline UI tests and HTTP/deployment validation.

These changes are prepared on a review branch. This entry does not imply that GitHub Pages has deployed them.
